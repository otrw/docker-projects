# ------------------------------------------------------------------------------
# Minecraft Server Environment Template
#
# Copy this file to .env before starting the container:
#   cp env.template .env
#
# Values in this file can be overridden by shell variables:
#   export EULA=true && docker compose up -d
# ------------------------------------------------------------------------------

# Required to accept Minecraft's EULA (must be 'true' to run)
EULA=true

# Java version only, required for GeyserMC container.
TYPE=PAPER

# Java version
VERSION=1.21.5

# Java Server Memory Allocation (e.g. 2G, 512M)
MEMORY=1G

# Game difficulty: peaceful, easy, normal, hard
DIFFICULTY=normal

# Game mode: survival, creative, adventure, spectator (Java only)
MODE=survival

# Message of the Day (MOTD)
MOTD=Welcome to my server!

# Timezone
TZ=Europe/London

# Enable RCON (Java only)
ENABLE_RCON=true

# RCON password (if RCON is enabled)
RCON_PASSWORD=changeme
